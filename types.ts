export interface UserType {
  id: string;
  name: string;
  email: string;
  activity: boolean;
  role: string;
}

export interface UserFormType {
  name: string;
  email: string;
  activity: boolean;
  role: string;
}